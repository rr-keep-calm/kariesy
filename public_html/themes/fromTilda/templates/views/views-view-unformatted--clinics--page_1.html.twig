<div class="clinics_content __base __base-2">
    {% if title %}
    <h4>{{ title }}</h4>
    {% endif %}
    <div class="clinics_items">
        {% for row in rows %}
            {{- row.content -}}
        {% endfor %}
    </div>
</div>